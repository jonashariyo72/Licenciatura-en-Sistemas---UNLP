package ar.edu.unlp.info.oo1.ej10_jobScheduler;

import java.util.List;

//public class StrategyHighestPriority implements Strategy{
   // public JobDescription next(List<JobDescription> jobs) {
    	// return (j1,j2) -> Double.compare(j1.getPriority(), j2.getPriority()) .orElse(null);
   // }

// }
